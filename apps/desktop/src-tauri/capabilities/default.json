{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-default",
  "description": "Minimum capability for the Stage 0 desktop window",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
